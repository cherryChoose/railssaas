== README
# 基于rack的web应用的性能调优
rack-mini-profiler 

# lock_version  如果异常抛出： ActiveRecord::StaleObjectError 

better_erros and panel rails (google engine)

throw errors  rescue_from

form object

rails cache

# 典型的用法在many-to-one时，当many端发生改变时，更新one端的updated_at时间。
belongs_to :article, touch: true

bcrypt # Use ActiveModel has_secure_password


STI (single table inheritance)